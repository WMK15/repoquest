# Optional. When set, RepoQuest uses OpenAI to generate the campaign and
# investigation narrative. Without it, the verified deterministic campaign
# is used and the full demo still works.
OPENAI_API_KEY=

# Optional model override (default: gpt-4.1)
OPENAI_MODEL=
